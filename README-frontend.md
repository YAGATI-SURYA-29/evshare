EVShare Frontend

Setup notes:
- Uses Bootstrap 5 via CDN in `base.html`.
- Static assets live under `static/` (css, js, images).
- Templates are Jinja-compatible for Flask/Django.

Quick test: serve with a minimal Flask app mounting templates/static.
